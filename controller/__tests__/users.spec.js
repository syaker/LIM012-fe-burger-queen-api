const {
  getUsers,
  getOneUser,
  addUser,
  deleteUser,
  updateUser
} = require('../../controller/users');

describe('getUsers', () => {
  it('should get users collection', (done) => {
    done();
  });
});
