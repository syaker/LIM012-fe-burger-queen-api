const {
	getProducts,
	getAnProduct,
	createAnProduct,
	updateAnProduct,
	deleteAnProduct
  } = require('../../controller/products');
  
  describe('getProducts', () => {
	it('should get products collection', (done) => {
	  done();
	});
  });
  describe('getAnProduct', () => {
	  it('should get products collection', (done) => {
		done();
	  });
	});
	describe('createAnProduct', () => {
	  it('should get products collection', (done) => {
		done();
	  });
	});
	describe('updateAnProduct', () => {
	  it('should get products collection', (done) => {
		done();
	  });
	});
	describe('deleteAnProduct', () => {
	  it('should get products collection', (done) => {
		done();
	  });
	});
  